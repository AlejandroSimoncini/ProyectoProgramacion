Bases de datos posibles:
Lista de Arboles AVL
Razones:
-La lista seria de ligas ya que son pocas
-Por nodo de lista habria un arbol separado
Preguntas para Tosetti:
radixsort
ejemplo de codigo 
#include<iostream>
#include<fstream>
#include<sstream>
#include<vector>
#include"GestorDeDatos.h"
#include"Hash/HashMapList.h"

// Aquí defines la función de Radix Sort
int getMax(const vector<int> &arr) {
    return *max_element(arr.begin(), arr.end());
}

void countSort(vector<int> &arr, int exp) {
    vector<int> output(arr.size());
    int count[10] = {0};

    for (int i = 0; i < arr.size(); i++)
        count[(arr[i] / exp) % 10]++;

    for (int i = 1; i < 10; i++)
        count[i] += count[i - 1];

    for (int i = arr.size() - 1; i >= 0; i--) {
        output[count[(arr[i] / exp) % 10] - 1] = arr[i];
        count[(arr[i] / exp) % 10]--;
    }

    for (int i = 0; i < arr.size(); i++)
        arr[i] = output[i];
}

void radixSort(vector<int> &arr) {
    int max_val = getMax(arr);
    for (int exp = 1; max_val / exp > 0; exp *= 10)
        countSort(arr, exp);
}

